# Headless service for StatefulSet DNS (pod discovery)
apiVersion: v1
kind: Service
metadata:
  name: lance-headless
  labels:
    app: lance
spec:
  clusterIP: None
  # Required: publish DNS for pods even before they're Ready
  # This allows peer discovery during startup
  publishNotReadyAddresses: true
  selector:
    app: lance
  ports:
  - name: client
    port: 1992
    targetPort: 1992
  - name: replication
    port: 1993
    targetPort: 1993
  - name: resync
    port: 1994
    targetPort: 1994
  - name: data-plane
    port: 1995
    targetPort: 1995
---
# LoadBalancer service for external access
apiVersion: v1
kind: Service
metadata:
  name: lance
  labels:
    app: lance
spec:
  type: LoadBalancer
  selector:
    app: lance
  ports:
  - name: client
    port: 1992
    targetPort: 1992
  - name: metrics
    port: 9090
    targetPort: 9090
  - name: health
    port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: lance
  labels:
    app: lance
spec:
  serviceName: lance-headless
  replicas: 3
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      app: lance
  template:
    metadata:
      labels:
        app: lance
    spec:
      securityContext:
        fsGroup: 1000
        runAsUser: 1000
        runAsGroup: 1000
      containers:
      - name: lance
        image: nitecon/lance:latest
        imagePullPolicy: Always
        securityContext:
          seccompProfile:
            type: Unconfined
          capabilities:
            add:
              - SYS_ADMIN
        command: ["/bin/sh", "-c"]
        args:
        - |
          NODE_ID="${HOSTNAME##*-}"
          exec /usr/local/bin/lance \
            --node-id "$NODE_ID" \
            --replication-mode l3 \
            --peers lance-0.lance-headless:1993,lance-1.lance-headless:1993,lance-2.lance-headless:1993 \
            --data-dir /var/lib/lance \
            --ingestion-actor-count 8 \
            --replication-max-inflight 64
        ports:
        - containerPort: 1992
          name: client
        - containerPort: 1993
          name: replication
        - containerPort: 1994
          name: resync
        - containerPort: 1995
          name: data-plane
        - containerPort: 9090
          name: metrics
        - containerPort: 8080
          name: health
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 20
          timeoutSeconds: 5
          periodSeconds: 10
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 10
          timeoutSeconds: 3
          periodSeconds: 5
          failureThreshold: 6
        resources:
          requests:
            memory: "512Mi"
            cpu: "2000m"
          limits:
            memory: "16Gi"
            cpu: "4000m"
        volumeMounts:
        - name: data
          mountPath: /var/lib/lance
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      storageClassName: ceph-block
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 32Gi
